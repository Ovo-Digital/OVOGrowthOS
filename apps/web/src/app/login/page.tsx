"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import Image from "next/image";
import { API_URL } from "@/lib/api";
export default function Login(){const r=useRouter();const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");const form=new FormData(e.currentTarget as HTMLFormElement);try{const res=await fetch(`${API_URL}/api/auth/login`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});if(!res.ok)throw new Error();const data=await res.json();localStorage.setItem("ovo_token",data.token);localStorage.setItem("ovo_user",JSON.stringify(data.user));r.push("/")}catch{setError("Giriş yapılamadı. Bilgilerinizi ve API'nin çalıştığını kontrol edin.")}}return <main className="grid min-h-screen place-items-center bg-[#101112] p-6"><form onSubmit={submit} className="card w-full max-w-[400px] p-7"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-[#101112] p-1.5"><Image src="/ovo-logo.svg" alt="OVO" width={48} height={48} priority/></div><h1 className="mt-5 text-2xl font-bold">OVO Growth OS&apos;a giriş yapın</h1><p className="mt-1 text-sm text-[#6d7175]">OVO Digital ekip çalışma alanı</p><label className="mt-6 block text-sm font-semibold">E-posta adresi<input name="email" type="email" className="input mt-1.5" defaultValue="admin@ovodigital.com" autoComplete="email"/></label><label className="mt-4 block text-sm font-semibold">Şifre<input name="password" type="password" className="input mt-1.5" defaultValue="ChangeMe123!" autoComplete="current-password"/></label>{error&&<p className="mt-3 text-sm text-[#d72c0d]">{error}</p>}<button className="mt-6 w-full rounded-lg bg-[#303030] py-2.5 text-sm font-semibold text-white">Giriş yap</button></form></main>}
